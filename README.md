# Google gemini built ADK simpple agent 
Able to to add othe to

run on terminal:
```bash
uv run adk run multi_tool_agent
```
run at web interface: 
```bash
uv run adk web --port 8000 multi_tool_agent
```